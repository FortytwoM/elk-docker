# Local environment — copy .env.example to .env and fill in your values
.env

# Runtime data (bind mounts, local state)
data/

# Generated TLS material (CA private key must never be committed)
tls/certs/**
!tls/certs/.gitignore
